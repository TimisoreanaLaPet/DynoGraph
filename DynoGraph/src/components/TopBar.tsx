import {AppBar, Button, Toolbar} from "@mui/material";

function TopBar() {
  return (
    <AppBar>
      <Toolbar>
        <Button variant={"text"}>
          test
        </Button>
      </Toolbar>
    </AppBar>
  );
}

export default TopBar;